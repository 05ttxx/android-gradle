/**
 * 依赖包的定义。
 * 
 * 这种定义方式的优点是在顶级项目目录下引入，在子项目中也可以直接用了。
 *
 */

// 一些会重复出现的jar包的版本号
// 
ext.versions = [
    //slf4j: '1.7.5',
    //httpclient: '4.3.1',
]

// 各种可能会用到的jar包
// 
ext.libraries = [
      //  "junit": "junit:junit:4.11"
]
